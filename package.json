{
  "private": true,
  "scripts": {
    "build": "cd packages/nft-models && yarn build && cd ../polkadot-chain-listener && yarn build"
  },
  "workspaces": [
    "packages/*",
    "api",
    "nft-indexer"
  ]
}
