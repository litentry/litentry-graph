{
  "private": true,
  "scripts": {
    "build": "cd nft-models && yarn build && cd ../chain-listener && yarn build"
  },
  "workspaces": [
    "chain-listener",
    "nft-indexer",
    "nft-models",
    "api"
  ]
}
