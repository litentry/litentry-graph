{
  "private": true,
  "scripts": {
    "build": "cd packages/nft-models && yarn build && cd ../polkadot-chain-listener && yarn build && cd ../nft-schema && yarn build",
    "postinstall": "yarn run build"
  },
  "workspaces": [
    "packages/*",
    "api",
    "nft-indexer"
  ]
}
